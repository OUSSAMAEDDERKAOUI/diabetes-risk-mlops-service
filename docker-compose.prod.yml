# version: "3.9"

# services:

#   api:
#     image: your_dockerhub_username/diabetes-risk-api:latest
#     container_name: diabetes-api
#     ports:
#       - "8000:8000"
#     restart: always

#   prometheus:
#     image: prom/prometheus
#     container_name: prometheus
#     volumes:
#       - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml
#     ports:
#       - "9090:9090"

#   grafana:
#     image: grafana/grafana
#     container_name: grafana
#     ports:
#       - "3000:3000"
version: "3.9"

services:

  mlflow:
    image: ghcr.io/mlflow/mlflow:latest
    container_name: mlflow-server
    ports:
      - "5000:5000"
    volumes:
      - ./mlruns:/mlflow/artifacts

  api:
    image: docker.io/oussamaedderkaoui/diabetes-risk-api:latest
    container_name: diabetes-api
    ports:
      - "8000:8000"
    depends_on:
      - mlflow
    environment:
      - MLFLOW_TRACKING_URI=http://mlflow:5000
    restart: always
